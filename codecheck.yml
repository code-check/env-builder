- hosts: localhost
  connection: local
  user: "{{ lookup('env','OS_USER_NAME') }}"
  sudo: no
  roles:
    - system
    - clang
    - gcc
    - go
    - gpp
    - gradle
    - groovy
    - haskell
    - java
    - perl
    - php
    - python3
    - ruby
    - scala
