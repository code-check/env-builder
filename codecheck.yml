- hosts: localhost
  connection: local
  user: "{{ lookup('env','OS_USER_NAME') }}"
  sudo: no
  roles:
    - system
    - php
    - ruby
    - node
    - java
    - scala
    - clang
    - gcc
    - go
    - gpp
    - groovy
    - haskell
    - ocaml
    - perl
    - gradle
