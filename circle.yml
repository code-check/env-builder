general:
  branches:
    only:
      - master
machine:
  services:
    - docker
dependencies:
  override:
    - docker version
    - docker info
test:
  override:
    - docker build -t all -f docker/all/Dockerfile docker/all
